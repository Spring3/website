const MARKERS = {
  shadow: 'var(--shadow-color)',
  red: 'var(--marker-red)',
  green: 'var(--marker-green)',
  yellow: 'var(--marker-yellow)',
  blue: 'var(--marker-blue)',
};

export {
  MARKERS
};
